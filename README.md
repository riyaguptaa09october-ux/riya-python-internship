# Riya Gupta — Python Developer Internship Tasks (internSpark)

3 of 4 tasks completed as required.

## Structure
```
task1_automation/       -> File Organizer & Cleaner (os module, logging, exception handling)
task2_api/               -> Weather Lookup Tool (Requests + JSON, search & filter)
task3_data_analysis/     -> Sales Data Analysis (Pandas — cleaning, grouping, insights)
```

## Task 1: Python Automation Script
Sorts files in a folder into subfolders by type, handles duplicates safely,
logs every operation. Run: `python file_organizer.py`

## Task 2: API Integration
Fetches live weather via `wttr.in`'s free JSON API. Supports single-city
search and multi-city filtering by temperature range or condition.
Run: `python weather_app.py`

## Task 3: Data Analysis (Pandas)
Loads a sales CSV, cleans missing values, groups by category/region,
and prints key business insights. Run: `python analyze_sales.py`
